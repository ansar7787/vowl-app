name: vowl
description: "Vowl: AI English Speaking Adventure"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.7

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.0.0
  equatable: ^2.0.7
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  firebase_app_check: ^0.4.3
  google_sign_in: ^6.2.2
  flutter_screenutil: ^5.9.3
  google_fonts: ^8.0.2
  dartz: ^0.10.1
  get_it: ^9.2.1
  internet_connection_checker_plus: ^2.9.1+2
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1
  audioplayers: ^6.5.1
  shared_preferences: ^2.5.4
  google_mobile_ads: ^7.0.0
  razorpay_flutter: ^1.4.1
  flutter_dotenv: ^6.0.0
  confetti: ^0.8.0
  haptic_feedback: ^0.6.4+3
  go_router: ^17.1.0
  firebase_analytics: ^12.3.0
  flutter_tts: ^4.2.5
  speech_to_text: ^7.1.0
  permission_handler: ^12.0.1
  flutter_animate: ^4.5.2
  share_plus: ^12.0.1
  lottie: ^3.1.2
  image_picker: ^1.2.1
  firebase_storage: ^13.3.0
  flutter_native_splash: ^2.3.1
  intl: ^0.20.2
  lucide_icons: ^0.257.0
  url_launcher: ^6.3.1
  package_info_plus: ^8.1.0
  path_provider: ^2.1.3
  collection: ^1.19.0
  firebase_crashlytics: ^5.2.0
  firebase_remote_config: ^6.4.0
  firebase_messaging: ^16.2.0
  flutter_local_notifications: ^18.0.1
  timezone: ^0.10.1
  flutter_timezone: ^5.0.2
  in_app_review: ^2.0.9
  safe_device: ^1.1.5
  cloud_functions: ^6.2.0
  clock: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  change_app_package_name: ^1.5.0
  mocktail: ^1.0.4
  bloc_test: ^10.0.0

flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "assets/images/vowl_logo.png"
  adaptive_icon_background: "#A8E063"
  adaptive_icon_foreground: "assets/images/vowl_icon_foreground.png"
  min_sdk_android: 21 # android min sdk min:16, default 21
  web:
    generate: false
    image_path: "assets/images/vowl_logo.png"
    background_color: "#A8E063"
    theme_color: "#A8E063"
  windows:
    generate: true
    image_path: "assets/images/vowl_logo.png"
    icon_size: 48 # min:48, max:256, default: 48

flutter_native_splash:
  color: "#FFFFFF"
  image: "assets/images/vowl_logo.png"
  branding: "assets/images/vowl_logo.png"
  color_dark: "#0F172A"
  image_dark: "assets/images/vowl_logo.png"
  branding_dark: "assets/images/vowl_logo.png"

  android_12:
    image: "assets/images/vowl_logo.png"
    icon_background_color: "#FFFFFF"
    image_dark: "assets/images/vowl_logo.png"
    icon_background_color_dark: "#0F172A"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - assets/images/
    - assets/sounds/
    - assets/sounds/correct.mp3
    - assets/sounds/wrong.mp3
    - assets/sounds/hint.mp3
    - assets/sounds/level_completed.mp3
    - assets/curriculum/accent/
    - assets/curriculum/grammar/
    - assets/curriculum/listening/
    - assets/curriculum/reading/
    - assets/curriculum/roleplay/
    - assets/curriculum/speaking/
    - assets/curriculum/vocabulary/
    - assets/curriculum/writing/
    - assets/curriculum/elite_mastery/
    - assets/curriculum/calendar/
    - assets/curriculum/kids/alphabet/
    - assets/curriculum/kids/numbers/
    - assets/curriculum/kids/colors/
    - assets/curriculum/kids/shapes/
    - assets/curriculum/kids/animals/
    - assets/curriculum/kids/fruits/
    - assets/curriculum/kids/family/
    - assets/curriculum/kids/school/
    - assets/curriculum/kids/verbs/
    - assets/curriculum/kids/routine/
    - assets/curriculum/kids/emotions/
    - assets/curriculum/kids/prepositions/
    - assets/curriculum/kids/phonics/
    - assets/curriculum/kids/time/
    - assets/curriculum/kids/opposites/
    - assets/curriculum/kids/day_night/
    - assets/curriculum/kids/nature/
    - assets/curriculum/kids/home_kids/
    - assets/curriculum/kids/food_kids/
    - assets/curriculum/kids/transport/
    - assets/curriculum/kids/body_parts/
    - assets/curriculum/kids/clothing/
    - assets/images/mascot/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
